# TODO

## 待办

- [ ] 给红魔提交蓝牙栈缺陷报告(抓包证据与六条证伪路径已齐,见 README)
- [ ] src/engine/tests.rs 728 行,略超 700 行的上限。按主题拆(连接决策 / 扫描 /
      提醒 / 日志)是自然的分法,但眼下只是一串互不相干的用例,不急
- [ ] 手柄闲置自动休眠:保活信号现在真的发得出去了(此前 connect() 在特权连接
      成功后直接返回,没建自己的 GATT 客户端,keepAlive 一直在空转)。还没验证
      它管不管用 —— 休眠由手柄固件决定,主机发的读请求算不算「有活动」只能实测。
      要验的话:开着游戏把手柄放一边十几分钟,看它会不会自己断

## 遇到了再验

这几条都要特定的真机条件才能触发,不专门去凑,日常用到时留意即可。

- [ ] 验证游戏中途掉线能否自动接回(需真机:游戏占前台时强制断开手柄)
- [ ] 验证平板重启后的完整流程(需真机:重启 → 手动启动 Shizuku → 确认应用等到服务就绪)
- [ ] 验证深度休眠下的回连(需真机:拔掉 adb,平板静置较长时间后重启手柄)
- [ ] 验证「设备久等不回」那条提醒通知(需真机:手柄关机静置 6 分钟以上,
      看通知栏是否弹出「已断开较久,可能需要开机」。蓝牙关闭、Shizuku 未就绪
      两条分支同理,都要先有设备久等才会开口)

## 已决定,不做

- 把引擎从界面拆进服务。布防继续随界面创建而初始化,前台服务只当「别冻我」的
  凭据。理由:Shizuku 重启后本来就得手动启动,反正要碰平板,拆分换不来真正的
  「开机即用」,不值这个架构改动。

## 长期

- [ ] 跟进 RedMagicOS 系统更新,确认后台回连缺陷是否被修复
