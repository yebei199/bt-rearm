# TODO

## 待办

- [ ] 验证游戏中途掉线能否自动接回(需真机:游戏占前台时强制断开手柄)
- [ ] 验证平板重启后的完整流程(需真机:重启 → 手动启动 Shizuku → 确认应用等到服务就绪)
- [ ] 验证深度休眠下的回连(需真机:拔掉 adb,平板静置较长时间后重启手柄)
- [ ] 给红魔提交蓝牙栈缺陷报告(抓包证据与六条证伪路径已齐,见 README)
- [ ] 决定要不要把引擎从界面拆进服务:现在布防随界面创建而初始化,进程被系统回收后
      服务单独活着也没用(已改为不自动重启,免得挂着假的「运行中」)。拆进服务能让
      布防独立于界面存活,但那是架构改动,等用户拍板。
      注意:开机自启不是理由 —— Shizuku 重启后本来就得手动启动,反正要碰平板

- [ ] 手柄闲置自动休眠:游戏进程还在时不该断。设想是每分钟给手柄发一次保活信号,
      但可行性未验证 —— 手柄的休眠由它自己的固件决定,BLE 主机能不能阻止要先查;
      另需确认「游戏进程还在」如何判定(前台应用?特定包名?用户自己指定?)

## 长期

- [ ] 跟进 RedMagicOS 系统更新,确认后台回连缺陷是否被修复
